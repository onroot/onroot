import { IEnvironment } from './environment.interface';

// Do not put real values for keys in this file.
// During development, Put real values in the environment.development.ts
// file and keep that file out of version control.

export const environment: IEnvironment = {
    production: true,
    GOOGLE_MAPS_API: 'undefined',
};
